{
  "${URL}": "http://${VAR;host}/api/v1/create",
  "${METHOD}": "POST",
  "${HEADER}": "",
  "${REQUEST}" : {
    "name":"${RETURN;AUTOGENERATE;C;9;name}",
    "salary":"${VAR;salary}",
    "age":"${VAR;age}"
    }
}
